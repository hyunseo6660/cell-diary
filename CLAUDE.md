# CLAUDE.md — 감정세포 일기앱

## 프로젝트 목표

드라마 '유미의 세포들' 컨셉의 AI 일기앱.
사용자가 일기를 쓰면, AI가 감정 세포들(불안, 분노, 이성, 회피, 욕망, 사랑 등)의 대화를 생성한다.
세포들의 대화를 통해 사용자는 하나의 감정에 휘둘리지 않고 자신의 감정과 생각을 다각도로 이해할 수 있다.

> 핵심 가치: 생각을 다중 관점으로 분리해 감정을 객관적으로 바라보는 경험 제공

---

## 핵심 기능

### 1. 일기 작성 & 세포 대화
일기 입력 → 상황/감정/의도 분석 → 세포들의 대화 생성 → 세포 비율 계산 → 오늘의 주인공 세포 선정 및 생각 정리

### 2. 세포 리포트
주간/월간 단위로 세포 활동을 분석하여 패턴 제공 (예: "이번 주 불안세포 증가 중", "회사 관련 기록에서 분노세포 증가 중")

---

## 감정 세포 목록

| 세포 | 역할 | 색상 |
|------|------|------|
| 불안세포 | 걱정, 두려움, 미래 불확실성 | 보라 |
| 분노세포 | 억울함, 화, 경계 침범 | 빨강 |
| 이성세포 | 논리, 분석, 판단 | 파랑 |
| 회피세포 | 도망, 미루기, 무기력 | 회색 |
| 욕망세포 | 원하는 것, 동기, 열망 | 주황 |
| 사랑세포 | 연결, 공감, 따뜻함 | 핑크 |

---

## 디렉토리 구조

```
감정세포 앱/
├── CLAUDE.md               # 이 파일 — 프로젝트 가이드
├── plan.md                 # 1-pager 기획안
├── package.json
├── server.js               # Express 서버 + Claude API 호출
├── .env.example            # 환경변수 템플릿
├── .env                    # 실제 환경변수 (gitignore)
├── .gitignore
├── index.html              # 메인 앱 페이지 (일기 작성)
├── report.html             # 세포 리포트 페이지
├── style/
│   ├── style.css           # 컴파일된 CSS
│   └── style.scss          # 소스 SCSS
├── js/
│   ├── app.js              # UI 로직 및 이벤트 핸들링
│   ├── api.js              # 서버 API 호출 클라이언트
│   └── cells.js            # 세포 캐릭터 데이터 및 렌더링 로직
└── asset/                  # 이미지, 아이콘, 세포 일러스트 등
```

---

## 기술 스택

| 역할 | 기술 |
|------|------|
| 프론트엔드 | Vanilla HTML / SCSS / JS |
| 백엔드 | Node.js + Express |
| AI | Anthropic Claude API (claude-sonnet-4-6) |
| 환경변수 | dotenv |
| 데이터 저장 | localStorage (MVP), 추후 DB 확장 |

---

## 일기 작성 플로우 (상세)

```
1. [일기 입력]
   → 오늘 있었던 일, 느낀 감정 자유 입력

2. [AI 분석 — Claude API]
   → 상황 파악: 어떤 일이 있었는가
   → 감정 추출: 어떤 감정들이 얽혀 있는가
   → 의도 분석: 사용자가 진짜 원하는 것은 무엇인가

3. [세포 대화 생성]
   → 각 세포가 자신의 관점에서 발언
   → 세포들 간 의견 충돌과 합의 과정
   → 자연스러운 대화 형식 (캐릭터성 유지)

4. [세포 비율 계산]
   → 오늘 일기에서 각 세포의 활성화 비율 (%)

5. [오늘의 주인공 세포 선정]
   → 가장 활성화된 세포
   → 오늘의 생각 정리 한 줄 요약
```

---

## API 설계

- `POST /api/diary` — 일기 분석 + 세포 대화 생성
- `GET /api/report` — 세포 리포트 데이터 조회
- 응답은 SSE(Server-Sent Events) 스트리밍 우선 구현

### 요청 형식
```json
{
  "content": "오늘 회사에서...",
  "date": "2026-05-02"
}
```

### 응답 형식
```json
{
  "analysis": { "situation": "", "emotions": [], "intent": "" },
  "dialogue": [
    { "cell": "불안세포", "message": "..." },
    { "cell": "이성세포", "message": "..." }
  ],
  "cellRatios": { "불안": 40, "이성": 30, "욕망": 20, "사랑": 10 },
  "protagonist": "불안세포",
  "summary": "오늘의 생각 정리 한 줄"
}
```

---

## 항상 수행할 규칙

### 코드 품질
- 모든 Claude API 호출은 `server.js`에서만 수행한다. 프론트엔드에서 API 키를 직접 노출하지 않는다.
- 환경변수는 반드시 `.env`에서 읽고, `.env`는 절대 커밋하지 않는다.
- Claude API 호출 시 반드시 prompt caching을 활용하여 비용을 절감한다.
- 새 기능 추가 전 기존 코드에서 재사용할 수 있는 부분을 먼저 확인한다.

### 세포 캐릭터 일관성
- 각 세포는 고유한 말투와 관점을 유지한다.
- 시스템 프롬프트에 각 세포의 페르소나를 명확히 정의하고 캐싱한다.
- 세포 이름과 색상은 `cells.js`에서 중앙 관리한다.

### 언어 및 UX
- UI 텍스트는 한국어 기본, 기술 주석은 한국어 또는 영어 허용.
- 세포 대화 문체는 각 세포의 성격을 반영하되 사용자가 공감할 수 있어야 한다.
- 입력이 짧거나 맥락이 부족해도 최선의 해석을 제공하며 에러를 사용자에게 전가하지 않는다.

### 스타일
- 모바일 first로 설계한다.
- 다크모드를 기본으로 한다.
- 각 세포는 고유한 색상과 아이콘으로 시각적으로 구분한다.
- 세포 대화는 말풍선 형태로 표시하여 드라마적 연출을 구현한다.

### 금지 사항
- 불필요한 npm 패키지 추가 금지.
- AI 응답을 그대로 텍스트로만 표시하지 말 것 — 세포 캐릭터 카드, 대화창 등으로 구조화.
- 주석으로 "TODO", "FIXME"를 남기지 말 것 — 즉시 처리하거나 plan.md에 기록.
